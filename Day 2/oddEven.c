#include <stdio.h>

void main(){
    int a = 10 ;
    a%2 == 0 && printf("a is an Even number\n") || printf("a is anOdd number\n");

     int b = 125 ;
    b%2 == 0 && printf(" b is an Even number\n") || printf(" b is an Odd number\n");
}